// Placeholder test — ensures Vitest runner is configured correctly.
// Real tests will be added in subsequent tasks.
import { describe, it, expect } from "vitest";

describe("setup", () => {
  it("vitest is configured", () => {
    expect(true).toBe(true);
  });
});
