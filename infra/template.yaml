AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: >
  Claro y Simple — Infraestructura serverless.
  Módulo 1: Ingesta y extracción de texto desde PDFs.
  Módulo 2: Motor de análisis de contratos con Amazon Bedrock.

# ---------------------------------------------------------------------------
# Parámetros
# ---------------------------------------------------------------------------

Parameters:
  EnvironmentName:
    Type: String
    Default: development
    AllowedValues:
      - localstack
      - development
      - production
    Description: >
      Modo del entorno. 'localstack' usa endpoints emulados localmente;
      'development' y 'production' apuntan a AWS real.

  BedrockModelId:
    Type: String
    Description: >
      Model ID (o inference profile ID) de Bedrock a usar por AnalysisFunction.
      Verificar disponibilidad regional y si requiere inference profile antes de
      cada deploy — el catálogo de Bedrock cambia con frecuencia.

# ---------------------------------------------------------------------------
# Mappings
# ---------------------------------------------------------------------------

Mappings:
  EnvironmentBucketSuffix:
    localstack:
      Suffix: localstack
    development:
      Suffix: dev
    production:
      Suffix: production

Globals:
  Function:
    Tags:
      project: hackaton

# ---------------------------------------------------------------------------
# Recursos
# ---------------------------------------------------------------------------

Resources:

  # -------------------------------------------------------------------------
  # API Gateway — REST API
  # -------------------------------------------------------------------------

  ClaroYSimpleApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: !Ref EnvironmentName
      Description: API de Claro y Simple
      Tags:
        project: hackaton
      # API Key requerida para control de costos durante el hackathon
      Auth:
        ApiKeyRequired: true
      # Throttling: rate 10 req/s, burst 20 (tech.md)
      MethodSettings:
        - ResourcePath: "/*"
          HttpMethod: "*"
          ThrottlingRateLimit: 10
          ThrottlingBurstLimit: 20

  # -------------------------------------------------------------------------
  # API Key y Usage Plan — requeridos porque Auth.ApiKeyRequired: true
  # -------------------------------------------------------------------------

  ClaroYSimpleApiKey:
    Type: AWS::ApiGateway::ApiKey
    DependsOn:
      - ClaroYSimpleApiStage
    Properties:
      Name: claro-y-simple-api-key
      Enabled: true
      StageKeys:
        - RestApiId: !Ref ClaroYSimpleApi
          StageName: !Ref EnvironmentName

  ClaroYSimpleUsagePlan:
    Type: AWS::ApiGateway::UsagePlan
    DependsOn:
      - ClaroYSimpleApiStage
    Properties:
      UsagePlanName: claro-y-simple-usage-plan
      ApiStages:
        - ApiId: !Ref ClaroYSimpleApi
          Stage: !Ref EnvironmentName
      Throttle:
        RateLimit: 10
        BurstLimit: 20
      Quota:
        Limit: 500
        Period: DAY

  ClaroYSimpleUsagePlanKey:
    Type: AWS::ApiGateway::UsagePlanKey
    Properties:
      KeyId: !Ref ClaroYSimpleApiKey
      KeyType: API_KEY
      UsagePlanId: !Ref ClaroYSimpleUsagePlan

  # -------------------------------------------------------------------------
  # Bucket S3 — almacenamiento de PDFs subidos
  # -------------------------------------------------------------------------

  ContractsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub
        - "claro-y-simple-contracts-${Suffix}"
        - Suffix: !FindInMap [EnvironmentBucketSuffix, !Ref EnvironmentName, Suffix]
      LifecycleConfiguration:
        Rules:
          - Id: expire-contracts-24h
            Status: Enabled
            Prefix: contracts/
            ExpirationInDays: 1
      Tags:
        - Key: project
          Value: hackaton

  # -------------------------------------------------------------------------
  # Tabla DynamoDB — ContractExtractions (Contrato 1)
  # -------------------------------------------------------------------------

  ContractExtractionsTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: ContractExtractions
      AttributeDefinitions:
        - AttributeName: document_id
          AttributeType: S
      KeySchema:
        - AttributeName: document_id
          KeyType: HASH
      BillingMode: PAY_PER_REQUEST
      TimeToLiveSpecification:
        Enabled: true
        AttributeName: ttl
      Tags:
        - Key: project
          Value: hackaton

  # -------------------------------------------------------------------------
  # Función Lambda — Ingestion handler
  # -------------------------------------------------------------------------

  IngestionFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: ingestion.handler.lambda_handler
      Runtime: python3.12
      CodeUri: ../backend/
      Timeout: 30
      MemorySize: 512
      Tags:
        project: hackaton
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: !Ref ContractExtractionsTable
          S3_BUCKET_NAME: !Ref ContractsBucket
          ENVIRONMENT: !Ref EnvironmentName
          LOG_LEVEL: INFO
      Policies:
        - S3WritePolicy:
            BucketName: !Ref ContractsBucket
        - S3ReadPolicy:
            BucketName: !Ref ContractsBucket
        - DynamoDBWritePolicy:
            TableName: !Ref ContractExtractionsTable
        - Statement:
            - Effect: Allow
              Action:
                - textract:DetectDocumentText
              Resource: "*"
      Events:
        IngestApi:
          Type: Api
          Properties:
            Path: /ingest
            Method: POST
            RestApiId: !Ref ClaroYSimpleApi

  # -------------------------------------------------------------------------
  # Tabla DynamoDB — ContractAnalyses (Contrato 2)
  # -------------------------------------------------------------------------

  ContractAnalysesTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: ContractAnalyses
      AttributeDefinitions:
        - AttributeName: document_id
          AttributeType: S
      KeySchema:
        - AttributeName: document_id
          KeyType: HASH
      BillingMode: PAY_PER_REQUEST
      TimeToLiveSpecification:
        Enabled: true
        AttributeName: ttl
      Tags:
        - Key: project
          Value: hackaton

  # -------------------------------------------------------------------------
  # Función Lambda — Analysis handler (Módulo 2)
  # -------------------------------------------------------------------------

  AnalysisFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: analysis.handler.lambda_handler
      Runtime: python3.12
      CodeUri: ../backend/
      Timeout: 60
      MemorySize: 256
      Tags:
        project: hackaton
      Environment:
        Variables:
          EXTRACTIONS_TABLE_NAME: !Ref ContractExtractionsTable
          ANALYSES_TABLE_NAME: !Ref ContractAnalysesTable
          BEDROCK_MODEL_ID: !Ref BedrockModelId
          MAX_CONTEXT_CHARS: "150000"
          BEDROCK_TIMEOUT_SECONDS: "45"
          ENVIRONMENT: !Ref EnvironmentName
          LOG_LEVEL: INFO
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref ContractExtractionsTable
        - DynamoDBCrudPolicy:
            TableName: !Ref ContractAnalysesTable
        - Statement:
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
              Resource: "*"
      Events:
        AnalyzeApi:
          Type: Api
          Properties:
            Path: /analyze
            Method: POST
            RestApiId: !Ref ClaroYSimpleApi

# ---------------------------------------------------------------------------
# Outputs
# ---------------------------------------------------------------------------

Outputs:
  IngestionApiUrl:
    Description: URL base del endpoint de ingesta
    Value: !Sub "https://${ClaroYSimpleApi}.execute-api.${AWS::Region}.amazonaws.com/${EnvironmentName}/ingest"

  IngestionFunctionArn:
    Description: ARN de la función Lambda de ingesta
    Value: !GetAtt IngestionFunction.Arn

  AnalysisApiUrl:
    Description: URL base del endpoint de análisis
    Value: !Sub "https://${ClaroYSimpleApi}.execute-api.${AWS::Region}.amazonaws.com/${EnvironmentName}/analyze"

  AnalysisFunctionArn:
    Description: ARN de la función Lambda de análisis
    Value: !GetAtt AnalysisFunction.Arn

  ContractAnalysesTableName:
    Description: Nombre de la tabla DynamoDB de análisis
    Value: !Ref ContractAnalysesTable
